import React from 'react';

import SectionHeading from './SectionHeading';

const Concepts = ({ mainContentHeading }) => (
  <SectionHeading mainContentHeading={mainContentHeading} />
);

export default Concepts;